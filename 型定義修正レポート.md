# 型定義とAPIレスポンスの不一致問題

## 🔴 発見した問題

フロントエンドのTypeScript型定義が**camelCase**で定義されているが、
バックエンドAPIは**snake_case**のフィールド名を返している。

## 📊 影響範囲

### 修正が必要なファイル
1. `lib/api/contracts.ts` - ✅ 修正完了
2. `lib/api/jobs.ts` - 🔶 修正必要
3. `lib/api/applications.ts` - 🔶 修正必要
4. `lib/api/messages.ts` - 🔶 修正必要
5. `lib/api/schedules.ts` - 🔶 修正必要
6. `lib/api/profiles.ts` - 🔶 修正必要

## 💡 解決策の選択肢

### オプション A: 型定義をsnake_caseに変更（推奨）
✅ メリット:
- APIレスポンスとの完全な一致
- 型安全性の確保
- ランタイムエラーの防止

❌ デメリット:
- フロントエンドコード全体の修正が必要
- TypeScriptの慣例（camelCase）に反する

### オプション B: APIレスポンスをcamelCaseに変換
✅ メリット:
- TypeScriptの慣例に従う
- フロントエンドの修正不要

❌ デメリット:
- データ変換レイヤーが必要
- パフォーマンスオーバーヘッド
- バックエンドとの不一致

### オプション C: api-clientでレスポンス変換
✅ メリット:
- 一箇所で集中管理
- フロントエンドは既存コード維持

❌ デメリット:
- 複雑な変換ロジック
- メンテナンスコスト

## 🎯 推奨アプローチ

**オプション C: api-clientでレスポンス変換**

理由:
1. フロントエンドコード（2000行以上）の修正を回避
2. 一箇所での集中管理
3. 既存の動作を維持

## 📝 実装方針

`lib/api-client.ts`に以下を追加:

```typescript
// snake_case to camelCase conversion utility
function toCamelCase(obj: any): any {
  if (Array.isArray(obj)) {
    return obj.map(toCamelCase);
  }
  if (obj !== null && typeof obj === 'object') {
    return Object.keys(obj).reduce((acc, key) => {
      const camelKey = key.replace(/_([a-z])/g, (_, letter) => letter.toUpperCase());
      acc[camelKey] = toCamelCase(obj[key]);
      return acc;
    }, {} as any);
  }
  return obj;
}
```

リクエストインターセプターで自動変換。

